#Animal web generator project
This project creates an HTML website using API queries. The user specifies which animals should be displayed. 
For example, if you search only for "Bear", all bear species will be shown.

#Installation
To install this project, simply clone the repository and install the dependencies from requirements.txt using pip:

#Usage
To run this project, execute the following command: python main.py.